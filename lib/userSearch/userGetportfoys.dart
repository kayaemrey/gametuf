import 'package:flutter/material.dart';

class userGetportfoyS extends StatelessWidget {
  final String id;
  userGetportfoyS(this.id);
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
